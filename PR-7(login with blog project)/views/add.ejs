<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Blog</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* General Styles */
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #d2ebff, #9fdbfc);       
            margin: 0;
            padding: 0;
        }

        a{
            text-decoration: none;
        }

        /* Header */
        .header {
            background: linear-gradient(to right, #2b5876, #4e4376);
            color: white;
            padding: 15px 30px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .header .logo {
            font-size: 1.8rem;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .header .logout-btn {
            background: linear-gradient(to right, #ff9966, #ff5e62);
            color: white;
            border: none;
            border-radius: 5px;
            padding: 8px 15px;
            font-size: 0.9rem;
        }

        .header .logout-btn:hover {
            background: linear-gradient(to right, #ff5e62, #ff9966);
        }

        /* Sidebar */
        .sidebar {
            height: 100vh;
            width: 260px;
            position: fixed;
            top: 0;
            left: 0;
            background: linear-gradient(to bottom, #232526, #414345);
            color: white;
            padding-top: 50px;
            overflow-y: auto;
            box-shadow: 2px 0 6px rgba(0, 0, 0, 0.1);
        }

        ul,li{
            list-style: none;
        }

        .sidebar .nav-link {
            color: white;
            font-size: 1rem;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 20px;
            transition: all 0.3s ease;
        }

        .sidebar .nav-link:hover {
            background: rgba(255, 255, 255, 0.2);
            border-radius: 5px;
        }

        .sidebar i {
            font-size: 1.2rem;
        }

        /* Main Content */
        .main-content {
            margin-left: 260px;
        }

        .main-content h1 {
            font-size: 2rem;
            padding: 30px;
            color: #4e4376;
        }

        .card {
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            background: linear-gradient(to bottom, #ffffff, #eef2f3);
        }

        .card-body {
            text-align: center;
        }

        .btn-primary {
            background: linear-gradient(to right, #6a11cb, #2575fc);
            border: none;
        }

        .btn-primary:hover {
            background: linear-gradient(to right, #2575fc, #6a11cb);
        }

        .btn-danger {
            background: linear-gradient(to right, #ff416c, #ff4b2b);
            border: none;
        }

        .btn-warning {
            background: linear-gradient(to right, #f7b42c, #fc575e);
            border: none;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
            }

            .main-content {
                margin-left: 0;
                padding: 20px;
            }
        }
    </style>
</head>

<body>

    <!-- Sidebar -->
    <div class="sidebar">
        <!-- <h1>Admin Panel</h1> -->
        <a href="/dashboard" class="nav-link"><i class="bi bi-speedometer2"></i> Dashboard</a>
        <a href="/add" class="nav-link"><i class="bi bi-file-earmark-plus"></i> Add Blogs</a>
        <a href="/manageusers" class="nav-link"><i class="bi bi-people"></i> Manage Users</a>
        <a href="/settings" class="nav-link"><i class="bi bi-gear"></i> Settings</a>
        <a href="/help" class="nav-link"><i class="bi bi-question-circle"></i> Help</a>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <div class="header">
            <div class="logo">Admin Panel</div>
            <a href="/logout" class="logout-btn">Logout</a>
        </div>

        <h1 align="center">Add a New Blog</h1>

        <!-- Add Blog Form -->
        <div class="card mx-auto" style="max-width: 600px; padding: 30px;">
            <form action="/addblouges" method="post" enctype="multipart/form-data">
                <div class="mb-4">
                    <label for="name" class="form-label">Blog Name:</label>
                    <input type="text" class="form-control" id="name" name="name" required>
                </div>
                <div class="mb-4">
                    <label for="description" class="form-label">Blog Description:</label>
                    <textarea class="form-control" id="description" name="description" rows="4" required></textarea>
                </div>
                <div class="mb-4">
                    <label for="image" class="form-label">Upload Image:</label>
                    <input type="file" class="form-control" id="image" name="image" required>
                </div>
                <div class="d-grid">
                    <button type="submit" class="btn btn-primary">Submit Blog</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
