<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Blog</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* General Styles */
        body {
            background: linear-gradient(to right, #2b5876, #4e4376);
            margin: 0;
            padding: 0;
        }

        a{
            text-decoration: none;
        }

        h1 {
            color: #fff;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            margin-bottom: 30px;
        }

        /* Header */
        .header {
            background: linear-gradient(to right, #2b5876, #4e4376); 
            color: white;
            padding: 15px 30px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .header .logo {
            font-size: 1.8rem;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .header .logout-btn {
            background: linear-gradient(to right, #ff9966, #ff5e62);
            color: white;
            border: none;
            border-radius: 5px;
            padding: 8px 15px;
            font-size: 0.9rem;
        }

        .header .logout-btn:hover {
            background: linear-gradient(to right, #ff5e62, #ff9966);

        }

        /* Card Design */
        .card {
            border: none;
            border-radius: 25px;
            background: linear-gradient(135deg, #d2ebff, #9fdbfc);       
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
            margin: 0 auto;
            max-width: 600px;
            overflow: hidden;
        }

        .card img {
            height: auto;
            border-top-left-radius: 25px;
            border-top-right-radius: 25px;
        }

        /* Button Styles */
        .btn-primary {
            background-color: #6a11cb;
            background-image: linear-gradient(to right, #6a11cb, #2575fc); /* Same gradient button */
            border: none;
            color: #fff;
        }

        .btn-primary:hover {
            background-image: linear-gradient(to right, #2575fc, #6a11cb); /* Hover effect */
        }

        /* Form Elements */
        .form-control {
            border-radius: 15px;
        }

        .form-label {
            font-weight: bold;
            color: #6a11cb; /* Matching color for labels */
        }

        .form-group {
            margin-bottom: 20px;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .card {
                width: 100%;
                margin: 20px 0;
            }
        }
    </style>
</head>

<body>
 
    <div class="header">
        <div class="logo">Admin Panel</div>
        <a href="/logout" class="logout-btn">Logout</a>
    </div>

    <div class="container py-5" style="padding-top: 1rem !important;">
        <h1 class="text-center">Edit Blog</h1>
        <div class="card">
            <form action="/up" method="post" enctype="multipart/form-data" class="p-4">
                <input type="hidden" value="<%= single.id %>" name="id">
                
                <div class="form-group">
                    <label for="name" class="form-label">Blog Name:</label>
                    <input type="text" class="form-control" id="name" name="name" value="<%= single.name %>">
                </div>

                <div class="form-group">
                    <label for="description" class="form-label">Blog Description:</label>
                    <textarea class="form-control" id="description" name="description" rows="4"><%= single.description %></textarea>
                </div>

                <div class="form-group">
                    <label for="image" class="form-label">Image:</label>
                    <input type="file" class="form-control" id="image" name="image">
                    <div class="mt-3 text-center">
                        <img src="/<%= single.image %>" alt="Current Image" class="img-fluid rounded" style="max-height: 150px;">
                    </div>
                </div>

                <div class="text-center">
                    <button type="submit" class="btn btn-primary">Update Blog</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
