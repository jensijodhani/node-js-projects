<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel - Add Product</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa; /* Light neutral background */
            margin: 0;
            padding: 0;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        /* Fixed Header */
        .header {
            background-color: #6583a1;
            color: white;
            padding: 1rem 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .header h1 {
            font-size: 1.8rem;
        }

        .header .user-info {
            font-size: 1rem;
        }

        /* Fixed Sidebar */
        .sidebar {
            background: #3b6a9a; /* Dark sidebar */
            color: white;
            min-height: 100vh;
            padding: 2rem 1rem;
            position: fixed;
            top: 0;
            left: 0;
            width: 250px;
            z-index: 999;
            box-shadow: 4px 0 6px rgba(0, 0, 0, 0.1);
        }

        .sidebar h2 {
            font-size: 1.75rem;
            margin-bottom: 2rem;
            text-align: center;
        }

        .sidebar .nav-links a {
            font-size: 1.1rem;
            color: white;
            margin-bottom: 1rem;
            display: block;
            padding: 0.6rem 1rem;
            border-radius: 8px;
            transition: background-color 0.3s ease;
        }

        .sidebar .nav-links a:hover,
        .sidebar .nav-links .active {
            background-color: rgba(255, 255, 255, 0.1);
        }

        /* Main Content */
        .main-content {
            margin-left: 270px;
            padding: 3rem 2rem 2rem 2rem;
            margin-top: 70px; /* To avoid content being hidden under header */
        }

        .card {
            background: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 30px;
            max-width: 600px;
            margin: auto;
        }

        .card h2 {
            text-align: center;
            margin-bottom: 2rem;
        }

        .card form {
            color: #333;
        }

        .card label {
            margin-bottom: 0.5rem;
        }

        .card input,
        .card textarea,
        .card button {
            border-radius: 8px;
        }

        .card button {
            background-color: #4CAF50;
            color: white;
            padding: 0.6rem 1rem;
            border: none;
            transition: background-color 0.3s ease;
        }

        .card button:hover {
            background-color: #45A049;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .header {
                flex-direction: column;
                align-items: flex-start;
                padding: 1rem;
            }

            .sidebar {
                width: 100%;
                padding: 1rem;
            }

            .main-content {
                margin-left: 0;
                padding: 1rem;
            }
        }
    </style>
</head>

<body>
    <!-- Fixed Header -->
    <header class="header">
        <h1>Admin Panel</h1>
        <div class="user-info">
            <span>Welcome, Admin</span>
        </div>
    </header>

    <!-- Fixed Sidebar -->
    <aside class="sidebar">
        <h2>Dashboard</h2>
        <div class="nav-links">
            <a href="/view"><i class="bi bi-view-list"></i> View Products</a>
            <a href="/add" class="active"><i class="bi bi-plus-circle"></i> Add Product</a>
            <a href="/logout"><i class="bi bi-box-arrow-right"></i> Log Out</a>
        </div>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
        <div class="card">
            <form action="/insertdata" method="post" enctype="multipart/form-data">
                <h2>Add New Product</h2>
                <div class="mb-3">
                    <label for="name" class="form-label">Product Name</label>
                    <input type="text" class="form-control" id="name" name="name" required>
                </div>
                <div class="mb-3">
                    <label for="price" class="form-label">Product Price</label>
                    <input type="text" class="form-control" id="price" name="price" required>
                </div>
                <div class="mb-3">
                    <label for="qty" class="form-label">Product Quantity</label>
                    <input type="text" class="form-control" id="qty" name="qty" required>
                </div>
                <div class="mb-3">
                    <label for="description" class="form-label">Product Description</label>
                    <textarea class="form-control" id="description" name="description" rows="4" required></textarea>
                </div>
                <div class="mb-3">
                    <label for="image" class="form-label">Upload Image</label>
                    <input type="file" class="form-control" id="image" name="image" required>
                </div>
                <div class="d-grid">
                    <button type="submit" class="btn">Submit Product</button>
                </div>
            </form>
        </div>
    </main>

    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
